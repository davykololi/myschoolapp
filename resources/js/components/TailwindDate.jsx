import React from 'react';
import ReactDOM from 'react-dom/client';
import ReactTailDate from './ReactTailDate';

export default function TailwindDate(){
    return (
            <div className="wrapper">
                <ReactTailDate/>
                gggggggggg
            </div>
            );
        }
    if (document.getElementById('tailwind')) {
    ReactDOM.render(<TailwindDate />, document.getElementById('tailwind'));
}

